# Configuração da API Backend
# Backend API Configuration

# URL do servidor backend (padrão: http://localhost:5150)
# Backend server URL (default: http://localhost:5150)
VITE_API_URL=http://localhost:5150

# Token de autenticação (opcional)
# Authentication token (optional)
# Se o backend exigir autenticação, adicione o token aqui
# If the backend requires authentication, add the token here
VITE_API_TOKEN=

# Exemplo com token:
# Example with token:
# VITE_API_TOKEN=seu-token-aqui
